The proposed method is in the `./TLAlgorithms/TLDoublySparseConditionedTL.m` file
and its routines are in `./TLAlgorithms/DoublyConditionedTLRoutines/` folder.

To test convergence, run `./test_convergence`.